import { createStyles } from "@mantine/core";

export const useStyles = createStyles(() => ({
  footer: {
    display: "flex",
    padding: "1rem",
    height: "70px",
    alignItems: "center",
    justifyContent: "right",
  },
}));
